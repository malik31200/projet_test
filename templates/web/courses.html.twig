{% extends 'base.html.twig' %}

{% block title %}Nos cours - Actual Sport{% endblock %}

{% block body %}
<h1 class="page-title">📋 Tous nos cours </h1>

{% if courses is empty %}
    <div class="card">
        <p>Aucun cours disponible pour le moment.</p>
    </div>
{% else %}
    <div class="grid">
    {% for course in courses  %}
        <div class="card">
            <h3>{{ course.name }}</h3>
            <p class="text-muted">{{ course.description }}</p>
            <div class="course-details">
                <div class="course-details-row">
                    <span>⏱️ Durée:</span>
                    <strong>{{ course.duration }} minutes</strong>
                </div>
                <div class="course-details-row">
                    <span>👥 Participants max:</span>
                    <strong>{{ course.maxParticipants }}</strong>
                </div>
                <div class="course-details-row">
                    <span> Prix:</span>
                    <strong>{{ course.price }}  €</strong>
                </div>
            </div>
            <a href="{{ path('app_sessions') }}" class="btn btn-full">
                Voir les sessions disponibles
            </a>
        </div>
    {% endfor %}
    </div>
{% endif %}
{% endblock %}